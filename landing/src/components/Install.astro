---
import { site } from '../data/site';
const { install, copy } = site;
---
<section id="install">
  <div class="w">
    <div class="section-kicker">Install</div>
    <h2>{copy.install.heading}</h2>
    <p class="lead" set:html={copy.install.leadHtml} />

    <div class="install-grid">
      <div>
        {install.entries.map((e) => (
          <div class="install-card">
            <div class="install-label">{e.label}</div>
            <div class="codeblock-wrapper has-copy single-line">
              <div class="cmd">{e.command}</div>
            </div>
            {e.hint && <div class="install-hint" set:html={e.hint} />}
          </div>
        ))}
        <p
          class="install-note"
          set:html={install.binariesNote}
        />
      </div>

      <div>
        <h3 class="try-title">Try it</h3>
        <div class="codeblock-wrapper has-copy">
          <div
            class="tryblock"
            set:html={install.tryCommands
              .map((c) => `<span class="dim">$</span> ${c}`)
              .join('\n')}
          />
        </div>
      </div>
    </div>
  </div>
</section>
