node_modules/
dist/
landing/dist/
.xtctx/.store/
.xtctx/config.yaml
.xtctx/skills/
.xtctx/state/
.claude/
.claude_disabled/
.codex/
.codex_disabled/
.cursor/
.cursor_disabled/
.gemini/
.antigravityignore
.vscode/mcp.json
.mcp.json
# ...except the one the plugin package ships. These entries exist because
# xtctx writes MCP config into whatever project it is set up in, including
# this one; `plugin/.mcp.json` is the opposite, a source file that Claude Code
# and Copilot CLI read out of the installed plugin. Without this it was
# silently left untracked and the published plugin registered no server on
# two of six clients.
!plugin/.mcp.json
.github/instructions/
# Anchored: this is opencode's own config at the repo root. Unanchored it also
# matched tests/drift/fingerprints/opencode.json, which silently stayed untracked.
/opencode.json
*.tsbuildinfo
coverage/
.env
.env.local
.artifacts/
firebase-debug.log
.playwright-mcp/
GEMINI.md.bak
home_page_screenshot.png
xtctx-v9-fullpage.png

# Local scratch dirs for smoke tests, reproduction harnesses, etc.
tmp/
.xtctx-tmp/

# Per-run artifact from ranking eval (only the committed baseline is tracked)
tests/eval/results/ranking-latest.json

# Checker output from the agent-skills acceptance gate
.agent-evidence/

# Next-gen implementation plan
IMPLEMENTATION_PLAN.md
