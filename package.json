{
  "name": "xtctx",
  "version": "0.10.0",
  "description": "Switch AI coding tools without restarting your project. Local memory + config layer for AI coding agents.",
  "type": "module",
  "bin": {
    "xtctx": "dist/src/cli/index.js"
  },
  "exports": {
    "./scrapers": {
      "types": "./dist/src/scrapers/index.d.ts",
      "import": "./dist/src/scrapers/index.js"
    }
  },
  "scripts": {
    "build": "tsc && node scripts/copy-static-assets.mjs",
    "dev": "tsx watch src/cli/index.ts",
    "test": "vitest run --exclude=tests/smoke/** --exclude=tests/drift/**",
    "test:security": "vitest run tests/api/security.test.ts",
    "test:integration": "vitest run tests/integration",
    "test:drift": "vitest run tests/drift",
    "drift:update-snapshots": "node scripts/run-with-env.mjs XTCTX_UPDATE_SNAPSHOTS=1 -- vitest run tests/drift",
    "test:watch": "vitest",
    "security:checklist": "node scripts/verify-security-checklist.mjs",
    "lint": "eslint src/ tests/",
    "mcp": "tsx src/mcp/server.ts",
    "api": "tsx src/api/server.ts",
    "landing:dev": "npm --prefix landing run dev",
    "landing:build": "npm --prefix landing run build",
    "landing:preview": "npm --prefix landing run preview",
    "prepack": "npm run build",
    "smoke:cli": "node dist/src/cli/index.js --help",
    "verify:release": "npm test && npm run test:security && npm run security:checklist && npm run test:drift && npm run build && npm run landing:build && npm run smoke:cli"
  },
  "dependencies": {
    "@clack/prompts": "^1.3.0",
    "@iarna/toml": "^2.2.5",
    "@lancedb/lancedb": "^0.13.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@xenova/transformers": "^2.17.2",
    "apache-arrow": "^17.0.0",
    "better-sqlite3": "^11.0.0",
    "chokidar": "^4.0.0",
    "commander": "^13.0.0",
    "express": "^5.0.0",
    "express-rate-limit": "^7.5.0",
    "glob": "^11.0.0",
    "helmet": "^8.1.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^10.0.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.56.1",
    "vitest": "^3.0.0"
  },
  "overrides": {
    "protobufjs": "^7.5.5"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fstubner/xtctx.git"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "license": "MIT",
  "homepage": "https://xtctx.com"
}
