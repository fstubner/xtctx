{
  "name": "xtctx",
  "version": "0.7.0",
  "description": "Cross-tool context for AI coding agents",
  "type": "module",
  "bin": {
    "xtctx": "dist/src/cli/index.js"
  },
  "exports": {
    "./scrapers": {
      "types": "./dist/src/scrapers/index.d.ts",
      "import": "./dist/src/scrapers/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "build:bundle-web": "npm run web:build && npm run web:copy-dist",
    "dev": "tsx watch src/cli/index.ts",
    "test": "vitest run --exclude=tests/smoke/** --exclude=tests/eval/** --exclude=tests/drift/** --exclude=tests/bench/**",
    "test:security": "vitest run tests/api/security.test.ts",
    "test:integration": "vitest run tests/integration",
    "test:eval": "vitest run tests/eval --testTimeout=300000",
    "test:drift": "vitest run tests/drift",
    "eval:baseline": "node scripts/run-with-env.mjs XTCTX_UPDATE_EVAL_BASELINE=1 -- vitest run tests/eval --testTimeout=300000",
    "drift:update-snapshots": "node scripts/run-with-env.mjs XTCTX_UPDATE_SNAPSHOTS=1 -- vitest run tests/drift",
    "bench:ingest": "node scripts/run-with-env.mjs BENCH=1 -- vitest run tests/bench --testTimeout=600000",
    "test:watch": "vitest",
    "security:checklist": "node scripts/verify-security-checklist.mjs",
    "lint": "eslint src/ tests/",
    "mcp": "tsx src/mcp/server.ts",
    "api": "tsx src/api/server.ts",
    "web:dev": "npm --prefix web run dev",
    "web:build": "npm --prefix web run build",
    "web:preview": "npm --prefix web run preview",
    "web:copy-dist": "node scripts/copy-web-dist.mjs",
    "landing:dev": "npm --prefix landing run dev",
    "landing:build": "npm --prefix landing run build",
    "landing:preview": "npm --prefix landing run preview",
    "prepack": "npm run build && npm run build:bundle-web",
    "smoke:cli": "node dist/src/cli/index.js --help",
    "test:smoke": "vitest run tests/smoke --testTimeout=180000",
    "verify:release": "npm test && npm run test:security && npm run security:checklist && npm run test:eval && npm run test:drift && npm run build && npm run build:bundle-web && npm run landing:build && npm run smoke:cli && npm run test:smoke"
  },
  "dependencies": {
    "@lancedb/lancedb": "^0.13.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@xenova/transformers": "^2.17.2",
    "apache-arrow": "^17.0.0",
    "better-sqlite3": "^11.0.0",
    "chokidar": "^4.0.0",
    "commander": "^13.0.0",
    "express": "^5.0.0",
    "express-rate-limit": "^7.5.0",
    "glob": "^11.0.0",
    "helmet": "^8.1.0",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.0",
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "eslint": "^10.0.3",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.56.1",
    "vitest": "^3.0.0"
  },
  "overrides": {
    "protobufjs": "^7.5.5"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fstubner/xtctx.git"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE",
    "CHANGELOG.md"
  ],
  "license": "MIT",
  "homepage": "https://xtctx.com"
}
